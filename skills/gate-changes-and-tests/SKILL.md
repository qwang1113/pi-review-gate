---
name: gate-changes-and-tests
description: 改门禁共享实现与给它写测试时的四条硬经验——改一个被多方复用的返回语义前必须枚举全部调用者并按类别各验收（同一根因已连续两轮酿成 P0）；循环/重试类被测对象要加调用次数 trip wire，否则变异体挂死而不是失败；凡是「从一个窗口里推导出事实或数出一个数」的东西（结构测试、`sed -n 'a,bp' | grep -c` 这类命令）都要先自证窗口真的盖住了两端，因为漏掉一半输入时它不报错、只会安静地小一点；一份自称与某个目录「一一对应」的表必须有一条双向差集测试，否则它只会单向漂移（加行有人做、删行和改计数没人做）。在修改 judge_wait / audit-round / 任何被 extension 与 lib 同时调用的函数前加载；在为循环、轮询、重试逻辑写测试前加载；在写「扫源码得出结论」这类结构测试前加载；在用行号窗口从文件里数任何数字、或准备引用别人给的数字之前加载；在新增或删除一个模块、以及维护任何自称「全量」的清单/速查表时加载。
---

# 改门禁与写测试

三条都指向同一件事：**门禁自己也是它自己的调用者**。改动看起来只影响 agent 那一侧，实际同时改掉了门禁内部那条没人盯着的链路。

## 1. 改共享实现的返回语义前，先枚举全部调用者

一个返回值被两类调用者消费时，「让 A 更好用」的改动往往同时**改坏了 B**——而 B 是门禁自己内部的链路，没有人会替它报错。

### 枚举法（三处都要查）

```
grep -rn 'callTool("<工具名>"' extensions/ lib/     # 1. 显式调用点
grep -rn '<registerXxx>' extensions/ lib/           # 2. internalHost 注册点
grep -rn '<工具名>' lib/*directives*.ts lib/*prompt*.ts  # 3. 注入文本里提到它的地方
```

枚举完不是就完了：**按调用者类别各验收一遍**。agent 侧走通了不代表门禁内部那条走得通。

### 实证 A（2026-09-05，修复 `c074b86`）

`judge_wait` 改成消息驱动后，打断了**门禁自己的 goal/plan 审计链**：审计链调一次 `judge_wait` 并把「不是 report」当成审计没跑完，而每个审计员在下结论前都会先流式吐 finding——于是第一条 finding 就把审计员中途关掉了，**任何带 findings 的草稿永远 fail closed**。修复 `c074b86`（`awaitRoundReport`：在消息驱动之上重调同一个工具直到本轮真正结束）。

### 实证 B（2026-09-05 稍后，同一根因再次命中，修复 `92056f7`）

把审计回合收敛成一个引擎（`dc6d886`）后，`judge_wait` 与同步审计链**共用了结论段**：wait 推进消费游标，同步链随后必然撞 `already-consumed`，于是**每一次 goal/plan 审计都 fail-closed**。修复 `92056f7`——它改用「一次记录必然留下的那对写入」（pending 被消费 **且** 游标前移）来判定「本轮已被 wait 收掉」。

这条值得点名的地方有三个：

1. **同一个根因连续两轮命中。** `c074b86` 修的是「wait 的语义变了，门禁自己的链没跟上」，`92056f7` 修的是「wait 和门禁自己的链共用了状态」——形状完全一样。
2. **它躲过了类型检查、全量测试和三遍通读**，最后是一次独立 `adviser` 咨询抓到的。
3. **结构性不可观测**：做这件事的会话跑的是**启动时加载的旧扩展**，它改的新代码根本不在自己的运行时里，所以「我跑一下试试」这条路在这里是不通的。

`92056f7` 的提交正文还点出了另一半根因，直接关系到第 2 条：

> The test fake now deletes the pending entry in forgetPending, matching the extension's dropAudits. **The unfaithful fake was what let the defect through.**

fake 与真实实现的行为不一致时，测试绿得毫无意义。改共享实现时，**同步检查每个 fake 是否还忠实**。

### 做法

改共享返回语义时，按顺序做完这四步：

1. 枚举三类调用点（上面的三条 grep）；
2. 给**每一类**调用者各写/各跑一条验收，不要只验 agent 那一侧；
3. 检查所有相关 fake 是否仍与真实实现同构；
4. 自己跑的运行时是旧扩展时，明说这一点，并用 `adviser` 咨询补上你看不见的那一半。

## 2. 循环类被测对象：加调用次数 trip wire

测一个「必须留最小间隔」的循环时，常用假时钟，而**假时钟只在 `sleep` 里推进**——这正是重点，暂停本身就是进展。但它带来一个后果：把间隔删掉的变异体**永远到不了 deadline**，于是测试**挂死**而不是失败。

### 实证

一次 reviewer 的变异运行因此**占满 CPU 22 分 43 秒**，最后靠人工 `pkill` 结束。修复 `b0875b3` 之后，同一个变异体在毫秒级失败并说明原因。

现在的写法在 `test/judge-lifecycle.test.ts:223`：

```ts
if (calls > 100) throw new Error("awaitRoundReport spun: the minimum gap is gone");
```

### 通则

被测对象里有循环、轮询、重试、退避时，除了断言「行为对」，再加一条**调用次数上限**。它不改变正确实现的任何行为，却把「挂死」变成「一句话说清的失败」——挂死会吃掉一次审查的全部预算，而且没人知道它在等什么。

## 3. 从源码窗口推导事实的结构测试：先自证推导，再断言结论

「扫源码得出一份清单，再拿清单下结论」这类测试有个隐蔽的失败模式：**推导漏掉一半输入时，结论恒为真，测试永远是绿的**。

### 实证

`agentVisibleTools()`（`test/extension-structure.test.ts:2443`，随 `c074b86` 引入）初版漏掉了一批真实注册的工具（当时的复盘记为 7 个；该计数未经逐一复核，但下列三处补偿本身可以直接读到），三个原因在最终实现里各留下一处补偿：

- **多行参数截断了窗口** → 正则放宽成 `pi\.registerTool\(\{\s*\n?\s*name: "([a-z_]+)"`；
- **链式注册**（一个 registrar 里再调下一个）→ 用 `queue` 做广度遍历；
- **`name` 是常量而非字面量**（`name: JUDGE_CONCLUDE_TOOL`）→ 回到声明它的模块里解析常量。

漏掉这些的版本，"没有任何注入文本提到不存在的工具" 这个结论照样成立——因为它压根没看见那些工具。

### 自证长这样

断言结论**之前**，先断言推导本身可用：它必须看见几个已知存在的项，且必须**看不见**一个已知的内部项（`test/extension-structure.test.ts:2489-2492`）：

```ts
const surface = agentVisibleTools();
assert.ok(surface.has("judge_submit") && surface.has("judge_wait") && surface.has("ask_user"),
  "the derivation itself must work before its verdict means anything");
assert.ok(!surface.has("judge_close"), "…and it must NOT see what only internalHost got");
```

正例证明它没漏，反例证明它没滥收。两条都要有——只有正例时，一个「把所有名字都收进来」的坏推导也能通过。

### 第二个实证（非测试版）：一个随手取的行号窗口，安静地少数了 8 行

这个失效模式不只在结构测试里。**任何「从窗口里数一个数」的动作都吃这一套**，而它在终端里比在测试里更隐蔽：测试至少还有个红绿，而 `grep -c` 只会给你一个**看起来很正常的整数**。

**实证（2026-09-05）**：一位项目经理要算 `README.md` 某个围栏块里 `lib/` 的覆盖率，给子会话的数字是 **19**；子会话自己数得到 **27**。两边命令只差一个窗口：

```
sed -n '1940,1990p' README.md | grep -c '^lib/'    → 19   # 随手取的窗口
sed -n '1926,1965p' README.md | grep -c '^lib/'    → 27   # 按围栏边界取
```

围栏块实际是第 `1925` 行到第 `1966` 行（两端都是围栏行），内容行 1926–1965。那个从 **1940** 起的窗口把开头 `1926–1939` 切掉了，而那一段里**恰好有 8 行** `lib/`——27 − 19 = 8。

最值得看的是那个窗口**两端都错了**，却只有一端改变了结果：

| 端 | 偏差 | 后果 |
|---|---|---|
| 尾端 1990 | 超出围栏 25 行 | **0 命中**，无害——你永远不会因此发现窗口是错的 |
| 头端 1940 | 比围栏晚 14 行 | **静静少数 8 行**，返回值仍是一个合理的整数 |

换句话说：**把窗口开大不会坑你，开小才会，而两者都不报错**。你拿到的永远是一个数，不是一个警告。

这个坑和上一节 `agentVisibleTools()` 漏掉真实工具是**同一个**：从窗口推导出的东西，漏掉一半输入时不会报错，只会安静地小一点。

### 从窗口取数前的两条自证法

1. **先定边界，再取行——别目测行号。** 先用一条不依赖行号的命令把两端定出来：

   ```
   grep -n '^```' README.md      # 先拿到围栏行号，再按它取行
   ```

   更好的是用**不依赖行号的判据**（awk 按围栏开关状态截、按标题截、按结构解析）——行号会随每一次编辑漂移，而漂移后它仍然返回一个整数。

2. **给窗口本身一个可观测的佐证——但要两端各放宽。** 取完数再确认窗口真的盖住了两端：首行/末行是不是你以为的那一行？然后**两端各放宽**重数一遍。

   **只放宽一端会给你一次假确认。** 用本节自己的实证试：原窗口 `1940,1990` 是**头端**截断，而尾端本就过宽，所以往下拉多远都没用——

   ```
   sed -n '1940,1990p' README.md | grep -c '^lib/'    → 19
   sed -n '1940,2050p' README.md | grep -c '^lib/'    → 19   # 尾端再放宽 60 行，纹丝不动
   sed -n '1940,2200p' README.md | grep -c '^lib/'    → 19   # 再放宽 150 行，还是 19
   sed -n '1900,2050p' README.md | grep -c '^lib/'    → 27   # 两端都放宽，才暴露
   ```

   偏偏「再往下拉一点」是最自然的动作，而它在头端截断时**恒定复现那个错数**。

   **数字变了，也别直接判成「原窗口在截断」。** 另一半可能是新窗口越界多收了本不该算的行。所以：**不一致只说明两个窗口里至少有一个是错的**——这时回到第 1 条去把边界定死，并核对多出来的那些行是不是本来就该在范围内。

   这就是上一节那两条 `assert.ok` 的命令行版本：正例（放宽后不该变）与反例（不该收进不属于它的东西），**两条都要**——只做一条，一个坏窗口照样能通过。

### 引用别人给的数字前，先自己数一遍

上面那个错数之所以被送出来，是因为子会话**没有直接转抄** 19，而是自己重数了一遍（两条命令互证：`grep -c` 与 `grep -o … | sort -u | wc -l`，均为 27）。

数字看起来比散文可靠，所以它更容易被直接转抄——**而一个错数字比一句错话更难被发现**。任务书、复盘、上游会话给的数，引用前都自己跑一遍；跑出来不一样就告诉对方，别静静改成自己的数，也别静静用对方的。

### 延伸：结构测试钉的是「形状」，重构时会成批失效

结构测试断言的是源码长什么样，不是行为是什么。所以一次正常的重构（把模块拆开、把措辞抽出去）会让它们**成批变红**，而这些红**不代表缺陷**。把审计回合收敛成引擎的 `dc6d886`、以及紧接着把措辞拆出去的 `e66e111`，就各踩到一次。

接受这个代价的前提是它在保护一件行为测试够不到的事（比如「注入文本不能提到不存在的工具」）。所以：

- 写的时候就把**这条测试保护的是什么**写进断言消息或注释，让下一个人能判断该修测试还是该修代码；
- 重构导致成批变红时，**逐条判断**是形状变了还是保护没了，不要整批放宽。

## 4. 自称「全量」的表，要有一条测试去比对真实目录

一份文档声称自己列全了某个目录，却只靠一句**人肉维护指令**维持（「新增或删除模块时，同一轮改动里顺手加/删这里的一行」）——它不会均匀地腐坏，而是**单向漂移**。

### 实证（2026-09-05，`docs/module-map.md` 的「`lib/` 全量速查表」）

发现时三个数两两不等：标题声称 **88**、实际模块 **107**、表里 **109** 行。做双向差集后，错得很不对称：

| 方向 | 结果 |
|---|---|
| `lib/` 有、表里没 | **0 条**——一个不缺，连当天新增的两个模块都已入表 |
| 表里有、`lib/` 没 | **2 条**幽灵行 |

**加行那半边人人做到了，删行和改计数那半边没有。** 原因很具体：新增模块时你正在写它，“顺手加一行”就在手边；删模块时你在另一个文件里干别的活，那张表不在你眼前。

### 更值得看的是：**尽责地处理也会破掉计数**

那两行幽灵的成因并不相同，而且两个删除 commit **都改过这张表**：

| commit | 对那一行做了什么 | 计数行 |
|---|---|---|
| `443ebfd`（删 `model-allowlist.ts`） | **完全没碰**，行还在把它描述成活模块 | 未改 |
| `651efed`（`verdict-parse.ts` 更名） | **认真处理了**——改写成墓碑「**已删除**（…）：…搬去 `precommit-parse.ts`」 | 未改 |

第二行才是重点：那个人**不但没偷懒，还多写了一句交代去向**。但墓碑**仍占一个计数行**，于是尽责的做法同样破掉了「条目数 = 目录文件数」。**「保留一条墓碑」与「表是全量清单」这两个意图从来没被对齐过**，而没有任何机制会把这个冲突摆到台面上。两个 commit 唯一共同的遗漏是**计数行**——因为它离你正在改的那一行最远。

### 结论：把维护指令变成测试

这张表自己就写着一条可机械执行的判据（「`ls lib/*.ts | wc -l`，与本表条目一一对应」）——**判据都写好了，却没人把它变成一条测试**。于是它只在有人真去敲那行命令时才招手，而那一天距它开始错已经过去很久了。

所以：**一份文档声称它与某个目录一一对应时，那句话就是一条待写的断言**。它不需要巧思，就是双向差集：

```ts
// 两个方向都要：少了说明漏登，多了说明有幽灵行
assert.deepEqual(inTableButNotOnDisk, [], "表里有、目录里没——幽灵行");
assert.deepEqual(onDiskButNotInTable, [], "目录里有、表里没——漏登");
```

只断言**条目数相等**是不够的：一条幽灵加一条漏登会互相抵消，数字照样对。这和§3「正例 + 反例两条都要」是同一个道理。

顺带一条改法上的纪律：修这类问题时，**别只把指标改对而把内容留着**。先前有人提议「只把 88 改成 107」，那会让表变成 109 行而声称 107——**比改之前更糟**，因为它把唯一能暴露问题的那个信号变成了绿的。**修一个指标时，别把发现问题的那个信号一起修没了。**
